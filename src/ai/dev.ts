import { config } from 'dotenv';
config();

// This will load the .env file and make the API keys available in process.env
// for local development. In production on App Hosting, secrets are injected
// automatically.
